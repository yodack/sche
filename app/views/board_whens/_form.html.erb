<%= form_for(@board_when) do |f| %>
  <% if @board_when.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@board_when.errors.count, "error") %> prohibited this board_when from being saved:</h2>

      <ul>
      <% @board_when.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :board %><br />
    <%= f.text_field :board %>
  </div>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.date_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
