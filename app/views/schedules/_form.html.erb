<%= form_for @schedules, url: {action: 'create', user_id: @user.id} do |f| %>
  <% if @schedules.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@schedules.errors.count, "error") %> prohibited this schedule from being saved:</h2>

      <ul>
      <% @schedules.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :board_when_id %><br />
    <%= f.text_field :board_when_id %>
  </div>
  <div class="field">
    <%= f.label :free %><br />
    <%= f.check_box :free %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
